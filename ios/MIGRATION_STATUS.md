# SilverCare iOS migration status

This directory is the active iOS migration for SilverCare AI Assistant. The goal is feature parity with the Android app, not a reduced demo.

## Required parity surface

| Android capability | iOS target implementation | Current status |
| --- | --- | --- |
| WebView UI with large controls, captions, management dashboard | `WKWebView` loads the existing `app/src/main/assets/index.html` bundle resources, uses `WebAssetLocator` to tolerate multiple bundle layouts, and injects an `AndroidSilverCare` compatibility object | Simulator automation and screenshot verified; AI reply/main-feedback throttling, main-feedback nonblocking layout, native-frame in-flight gating, and TTS fallback noise suppression are covered by JS/XCUITests. The opt-in physical-device UI debug gate covers ordinary launch, settings, AI details, management round trip, and now includes a start-navigation native-camera-flow screenshot test for unlocked devices |
| Native bridge messages | `WKScriptMessageHandler` routes JS calls into Swift services and returns messages through `LONG_TERM_CARE_NATIVE_MESSAGE`; the iOS bridge now also exposes native camera status, authorization state, hardware availability, error codes, and frame-processing/busy states for automation/debugging | Bridge coverage, camera status/locking route coverage, default simulator XCUITest, and cloud-first device smoke verified; physical-device camera-preview and live mic interaction evidence remains separate |
| Native settings menu | iOS settings now covers runtime mode, DashScope Key, region/model fields, offline model checks/preparation, captions, voice-first mode, fall detection, navigation refresh, SME2 tuning, status push, and speech test; action sheets are iPad-safe with popover anchors | Code integrated; simulator settings subflows verified and physical-device UI debug exercises the ordinary settings entry |
| Navigation frame processing | `SilverCareProcessor.processFrame` ported to Swift core; iOS app processing now runs through `SilverCareProcessorPipeline` actor instead of blocking the `@MainActor` app model | Core verified; simulator UI verifies tapping "启动导航" triggers the native camera/fallback frame path |
| Search intent closure | Voice/text inquiry sets `currentGoal` and immediately runs navigation on the same frame | Core verified |
| Precision guidance | Explicit "引导" starts micro mode; close keywords exit it; manual refresh taps now continue to work in micro mode | Core verified; JS behavior tests cover manual micro refresh, micro result UI, spatial audio, and speech throttle |
| Task guidance | Task planning and task controls ported in Swift core | Core verified |
| Local ASR correction | Common ASR correction rules and model-response parser ported | Core verified |
| Fall confirmation | Sensor plus visual-evidence confirmation core ported; iOS native CoreMotion fallback shows a native confirmation dialog and triggers the same `fall_alarm` bridge event after countdown | Core verified, native fallback integrated, pending device validation |
| Smart refresh | Semantic consistency check ported | Core verified |
| Local offline model status/download | `OfflineModelManager` mirrors the Android Qwen3-4B + DAMO-YOLO manifest, checks model readiness, separates component-level `vision_ready` and `text_inference_ready` state, copies the bundled detector, downloads Qwen files with resumable `.part` files, and reports `model_download_progress` to WebView. The app-level prepare flow now also prepares the Android-compatible Vosk Chinese ASR model in the same progress stream | Core verified, App bridge integrated; pending real large-model download/device validation |
| Local offline vision | DAMO-YOLO detection interpretation ported; model-file readiness is reported in iOS settings; `DynamicIOSMNNLocalModelRuntime` now prefers an Android-compatible 640x640 RGB CHW float C ABI (`silvercare_mnn_vision_json_from_chw`) and keeps data-URL ABI fallback; `ios/Native/SilverCareMNNRuntimeABI.h` documents the native contract; the app can load MNN symbols from the main image, `SILVERCARE_MNN_LIBRARY`, or bundled `SilverCareMNNRuntime.framework`/`libsilvercare_mnn_runtime.framework`/`.dylib` artifacts. The official Alibaba MNN 3.5.0 iOS arm64 static framework is vendored under `ios/Native/Vendor/MNN-3.5.0-ios`, and `ios/Native/Vendor/SilverCareMNNRuntime.xcframework` now exports the SilverCare MNN C ABI for iPhoneOS arm64 | Core interpreter, model status, dynamic loader path, official iOS MNN core dependency, SilverCare MNN wrapper symbols, and device app embedding verified; pending real model inference validation and simulator runtime slice |
| Local offline LLM | Swift service boundary defined; Qwen3-4B model preparation is implemented; dynamic C ABI route for text inference and SME2 tuning config is scaffolded; optional MNN runtime artifacts under `ios/Native` are copied into the app bundle by Xcode when present; the SilverCare iOS MNN wrapper links the vendored MNN core and exports text/vision ABI symbols | Model preparation, official MNN iOS core dependency, wrapper link, and loader/embed path verified; pending real on-device text inference validation |
| Local ASR | `LocalASRModelManager` now mirrors Android's Vosk small Chinese model directory contract and required files; iOS downloads the official `vosk-model-small-cn-0.22.zip`, resumes `.part` downloads, safely extracts stored/deflated ZIP entries, rejects unsafe paths, and exposes model path/missing/runtime state through the runtime bridge. `LocalVoskASRRuntime` dynamically loads `libvosk.framework`/`libvosk.dylib` or probes `dlopen(nil)` for directly linked `libvosk.a` symbols, calls the Vosk C API with Android-compatible 16k PCM chunks, parses Vosk final JSON, and the local ASR path no longer falls back to Apple Speech. `ios/Tools/install_vosk_runtime_artifact.sh` now converts static Vosk archives/xcframeworks into an embeddable dynamic `libvosk.xcframework`; `ios/Native/Vendor/libvosk.xcframework` exports the required Vosk C symbols for iPhoneOS and simulator arm64. Simulator automation now seeds the Vosk model and a 16kHz benchmark WAV into the app container, and the ASR report must produce non-empty transcripts | Model download/extraction, runtime boundary, Vosk dynamic xcframework packaging, strict runtime gate, source contracts, and simulator real-audio Vosk transcription verified; real-device microphone transcription still pending |
| TTS | iOS `AVSpeechSynthesizer` is the main equivalent to Android system TTS; it sets a spoken-audio playback session before speaking, deactivates it after finish/cancel, and returns TTS speaking state to WebView. DashScope/local TTS fallback notices are treated as speech-channel degradation and no longer overwrite the AI reply or main feedback overlays. DashScope URL playback and local MNN WAV playback now share an AVPlayer path that listens for normal completion, interrupted playback, and failed item loading; failures write diagnostics, reset WebView TTS state, and fall back to iOS system TTS. The local MNN TTS experimental path now mirrors Android's `bert-vits2-MNN` model manifest, checks/downloads the 23-file model layout, exposes real `local_tts_*` status through the bridge, and has a dynamic iOS MNN TTS ABI loader for `silvercare_mnn_tts_*` symbols. `ios/Tools/build_mnn_tts_runtime_wrapper.sh` now builds a device `arm64` `SilverCareMNNTTSRuntime.xcframework` from the vendored MNN TTS source and exports the SilverCare TTS ABI. The local TTS benchmark now calls the native ABI to synthesize two Chinese WAV files whenever the model/runtime/voice-quality gate reports ready; the report records each WAV path, readability, size, and elapsed time, and the checker rejects a ready TTS runtime that does not actually synthesize audio. Runtime/voice-quality failure remains explicit instead of entering the main speaking path | System/DashScope code integrated; playback failure fallback integrated; local MNN TTS model side, dynamic runtime symbols, device app embedding, and ready-time WAV benchmark gate verified; pending real model synthesis and voice-quality validation on device |
| DashScope cloud mode | `DashScopeAIClient` supports OpenAI-compatible text/vision, multimodal ASR, and TTS URL synthesis with injectable transport; iOS settings can store Key and switch runtime; iOS now records mic audio as 16kHz mono WAV data URLs for DashScope ASR | Protocol verified; live text/vision/ASR/TTS smoke, downloaded TTS audio-file validation, strict live-scenario report validation, Key-injected simulator automation, Key-injected device smoke, and the umbrella migration verifier passed. Real device long-press mic recording remains an interaction-level follow-up |
| Diagnostics | `IOSDiagnosticLogger` writes JSONL session/latest logs in the app documents directory, accepts JS bridge events, and exposes the latest log path through `AndroidSilverCare.diagnosticLogPath()`. iOS also has a settings entry for local model diagnostics; `AndroidSilverCare.runLocalBenchmark(test)` writes Android-compatible `status/asr/vision/text/text_suite/text_inquiry/tts/scenario` reports under `Documents/benchmarks/latest-{test}.json`, exposes the latest benchmark path through the bridge, and includes the current `diagnostic_log_path`, native camera status/permission/hardware/error-code, native speech mic authorization/ASR runtime/recording state, native TTS runtime/system-speaking/audio-playback/local-MNN readiness state, plus component-level offline model readiness fields in the reports | Code integrated; simulator report validation requires diagnostic log paths, native camera diagnostics, native speech diagnostics, and native TTS diagnostics on every benchmark report; pending device log export and native runtime benchmark validation |
| Real iOS device validation | Requires physical-device camera/mic permission checks, on-device screenshot/log export, and native runtime validation | Signed build/install/launch and device artifact export verified; real camera preview interaction, microphone recording, and full model-backed inference still need device data validation |

## Evidence produced in this pass

- `swift test` in `ios/`: 58/58 Swift Package tests passed.
- `npm run check:js` checks JavaScript syntax, verifies all 40 Android `@JavascriptInterface` methods plus every frontend `AndroidSilverCare`/`safeNative*` method reference exist in the iOS bridge script, confirms `void` Android bridge actions have Swift route cases, confirms iOS local benchmark coverage for all 8 Android `LocalModelBenchmarkActivity` test branches, runs the iOS native runtime source-contract check, runs `tools/check-ios-permissions.mjs` to compare Android manifest camera/microphone/network/audio/TTS declarations with iOS privacy strings, request paths, audio session use, optional hardware handling, and motion privacy, runs `tools/check-ios-tts-playback.mjs` to keep DashScope/local MNN TTS AVPlayer completion, failure, diagnostic, state-reset, and system-TTS fallback handling in place, and runs `tools/check-no-sensitive-secrets.mjs` so live DashScope keys cannot be left in the repository or generated reports.
- `npm run test:js`: 35/35 JavaScript tests passed, including rapid AI caption throttling, duplicate AI reply/main-feedback suppression across scan intervals, long main-feedback compression, native camera frame in-flight gating, camera warming/busy/error lock release, native speech-status/error recovery of the hold-to-inquire button UI, native fall-alarm UI plus management risk-queue synchronization, explicit iOS native-camera CSS mode gating, TTS fallback suppression, ASR error surfacing, runtime-status TTS subtitle exposure, manual micro-guidance tap refresh, and `micro_result` UI/spatial-audio/speech-throttle checks.
- `xcodegen generate` creates `ios/SilverCareiOS.xcodeproj`.
- `ios/Tools/run_simulator_automation.sh` on the `SilverCare Test iPhone 15` simulator is now the default simulator gate for `npm run test:ios:sim`. The latest full run passed JS checks, 35 JS tests, 58 Swift Package tests, 67 simulator Xcode unit tests, and 9 simulator UI tests (`SilverCareiOSUITests/SilverCareiOSUITests`; `xcresult` at `ios/build/DerivedData-SimulatorAutomation/Logs/Test/Test-SilverCareiOS-2026.06.24_13-35-05-+0200.xcresult` reports 76 total tests and no failures), then built/installed/launched the simulator app, seeded the cached `vosk-model-small-cn-0.22` model plus 16kHz `manual_test/real_voice.wav`, `real_scene.jpg`, and the DAMO-YOLO detector into the app container, and ran `tools/check-ios-benchmark-reports.mjs` with strict ASR/scenario-fixture, diagnostic-log-path, native-camera, native-speech, native-TTS, and component-level offline-status requirements. The latest `latest-asr.json` reports two successful Vosk runs (`cold_manual_real_voice_vosk` and `warm_manual_real_voice_vosk`) with transcripts `帮我找门`; all 8 latest benchmark reports now include `diagnostic_log_path`, `native_camera`, `native_speech`, and `native_tts` diagnostics, including ASR runtime, microphone authorization, recorder state, local ASR readiness, TTS runtime, system-speaking state, AVPlayer-backed native audio playback state, DashScope TTS availability, and local MNN TTS readiness/voice-quality gate; `latest-status.json` / `latest-vision.json` / `latest-scenario.json` report `yolo_ready=true`, `vision_ready=false` only because `MNN Native Runtime` is unavailable in the simulator, `text_inference_ready=false` because `MNN Native Runtime` plus `Qwen3-4B-Instruct-2507-MNN/config.json` are missing, and `mnn_sme2_supported=false` while the runtime is not loaded. `latest-scenario.json` also runs the scenario ASR component (`manual_real_voice_vosk`) and records transcript `帮我找门`, while keeping the overall scenario failed with explicit `vision_skipped` / `pipeline_skipped` reasons until MNN text/vision can execute. The run also wrote `ios/build/simulator-automation/silvercare-home.png` plus `ios/build/simulator-automation/simulator-recent.log`; the screenshot shows the AI reply panel clamped to two lines, the TTS Key fallback notice absent from the reply area, and all three bottom commands visible.
- XCUITest now verifies that the home screen controls are actually hit-testable (`home-controls-hittable`, `hit-toggle`, `hit-settings`, `hit-management`), that the AI details close button is hit-testable (`hit-close-details`), that the AI reply/caption panel is fixed, clamped, and nonblocking (`caption-layout-ok`), that the main-feedback overlay is noninteractive and clear of the bottom controls (`main-feedback-layout-ok`, `main-feedback-clear-of-controls`, `main-feedback-nonblocking`), that the runtime subtitle exposes the current TTS scheme (`tts-runtime-visible`), and that TTS fallback notices do not pollute main feedback, user captions, or AI captions (`feedback-tts-clean`, `main-feedback-tts-clean`, `user-caption-tts-clean`, `ai-caption-tts-clean`). The home-control regression now presses inquiry to force visible feedback, then opens/closes AI details and round-trips through the management dashboard while reasserting that the main controls remain hittable. The startup/navigation test now distinguishes native camera availability from simulator/web fallback by asserting `camera-bridge-*`, `camera-hardware-*`, `camera-native-*`, `camera-preview-*`, `camera-auth-*`, and `camera-error-*` automation tokens after tapping "启动导航"; if iOS reports `camera-native-running`, the test also requires `camera-preview-visible` / `camera-native-preview-running`, so a Web-only status update cannot masquerade as a visible native camera. The UI tap helper now prefers real buttons/switches/cells/interactive elements instead of broad static-text matches, and 11 native settings subflows can actually be tapped in the simulator: runtime mode, all-local switch, DashScope region/model, offline text model, ASR mode, TTS mode, offline model check, speech/captions/fall, navigation refresh mode, SME2 tuning, and local model diagnostics.
- The iOS app model no longer calls the blocking DashScope/offline processor synchronously on `@MainActor`. `captureFrame`, speech inquiry, and text inquiry now dispatch to `SilverCareProcessorPipeline`, and the JS native-camera loop keeps only one native frame request in flight until a result, warming, busy, error, or timeout status returns. Processor failures now return `frame_status:error` to release the frame lock while preserving the camera runtime state, so an AI/model/Key failure no longer masquerades as native camera `frame_error`.
- `RuntimeStatusSnapshotStoreTests` now run inside the app-hosted Xcode unit target and verify that the runtime status snapshot remains whole under concurrent reads/writes, that `IOSHybridAIClient.settings` can read the latest runtime mode from a background thread without touching `@MainActor` state, that missing iOS MNN runtime does not return synthetic offline vision/text JSON as if inference had succeeded, and that missing MNN symbols report `supportsSme2=false`.
- `npm run test:ios:device` now runs `ios/Tools/run_device_smoke.sh`: it detects the connected iPhone `00008101-0015519C11BA001E`, regenerates the project, runs bridge/native-runtime/parity/privacy/TTS checks, writes a signing preflight report to `ios/build/device-smoke/signing-preflight.txt`, first builds an unsigned generic iPhoneOS app at `ios/build/DerivedData-device-nosign` and requires that `.app` bundle itself to contain `libsilvercare_mnn_runtime.dylib`, `libsilvercare_mnn_tts_runtime.dylib`, and `libvosk.dylib` as `IOS` platform slices, then performs the signed device build/install/launch and copies benchmark plus diagnostic artifacts. The persistent `ios/project.yml` signing team is now `887TZ78HJ5`; the latest preflight reports `project_team=887TZ78HJ5`, `effective_team=887TZ78HJ5`, and `development_team_override=<none>`, so the device pass no longer depends on a command-line signing override. Device smoke is cloud-first by default: it launches the app with `SILVERCARE_IOS_FORCE_DASHSCOPE_RUNTIME=1`, runs the `status` benchmark only, and asserts that `latest-status.json` reports ASR/TTS runtime modes as `dashscope`; when `DASHSCOPE_API_KEY` is provided, it also asserts `native_tts.dashscope_available=true` without writing the Key to artifacts. Local ASR/model fixture seeding is now opt-in with `SILVERCARE_PREPARE_DEVICE_LOCAL_ASR=1` and `SILVERCARE_IOS_LOCAL_BENCHMARK_TESTS=...`. The latest Key-injected device smoke is `status=passed`, `unsigned_runtime_preflight=passed`, and `signed_build_status=passed`; `latest-status.json` reports `success=true`, `native_speech.asr_runtime_mode=dashscope`, `native_tts.tts_runtime_mode=dashscope`, `native_tts.dashscope_available=true`, and status text confirms the DashScope Key is configured. `npm run test:ios:device-ui` is the opt-in physical-device screenshot/tap gate; it now writes `running` at start, has a log watchdog for locked-device / automation-timeout waits, enables the automation snapshot channel on device debug runs, and covers launch, settings, AI details, management round trip, plus start-navigation native camera flow. The latest device-ui run is `status=passed` with 3 tests and 0 failures.
- `DashScopeAIClient` protocol paths for text, vision, ASR, and TTS are covered by the verifier using a fake transport; `npm run test:dashscope:scenarios` additionally verifies live DashScope text, street-image vision JSON, benchmark-audio ASR, and TTS synthesis with a real Key, downloads the generated TTS WAV to `test_runs/live_dashscope_scenarios/audio/`, then runs `tools/check-live-dashscope-scenarios.mjs` to validate `summary.json`, downloaded image/audio files and byte counts, navigation JSON fields, ASR/TTS smoke fields, expected street-navigation hints, and Key redaction.
- `tools/check-ios-bridge.mjs` confirms iOS bridge compatibility against both Android `@JavascriptInterface` methods and current frontend native method references; the latest run checked 40 Android bridge methods, 8 Android benchmark branches, and 47 frontend native method references.
- `npm run verify:ios:migration` is now the umbrella migration verification command. It writes `ios/build/migration-verification/summary.json` and per-gate logs. With `DASHSCOPE_API_KEY` present, the latest run executed `check:js`, `test:js`, live DashScope scenarios, simulator automation, signed device smoke, and device-summary validation; the aggregate summary is `status=passed` and can be rechecked with `npm run check:ios:migration-summary`. `npm run check:ios:online-chain` is the cloud-path evidence gate: it requires that same live migration summary, the live DashScope report with downloaded network street images plus ASR/TTS artifacts, the simulator `latest-status.json` plus screenshot artifact, and the signed device smoke `latest-status.json` showing DashScope ASR/TTS configured and available.
- `tools/check-ios-native-runtime.mjs` confirms that the iOS MNN, MNN TTS, and Vosk runtime loaders and bridge expose the expected contracts. `SILVERCARE_REQUIRE_IOS_NATIVE_RUNTIME=1 node tools/check-ios-native-runtime.mjs` now passes with all three native runtimes available (`iOS MNN runtime`, `iOS MNN TTS runtime`, and `iOS Vosk runtime`). Simulator and device smoke scripts also invoke it against the built `.app` bundle through `SILVERCARE_IOS_APP_PATH`; the checker now reports app-bundle runtimes separately from source-tree candidates, and `SILVERCARE_REQUIRE_IOS_APP_BUNDLE_RUNTIME=1` fails unless the built app itself contains the required iOS-platform runtime slices.
- Native runtime artifact verification now audits candidate symbol sets, device `arm64` slices, and Mach-O platform slices, reports the official MNN iOS core dependency separately from the final SilverCare ABI runtime, and the Xcode embed phase can select matching device/simulator slices from `.xcframework` artifacts. The latest native runtime set includes `libsilvercare_mnn_runtime.dylib`, `libsilvercare_mnn_tts_runtime.dylib`, and `libvosk.dylib`; the simulator app bundle still correctly reports no bundled MNN or MNN TTS simulator runtime, while Vosk provides an arm64 simulator slice and has now been exercised with real benchmark audio in the simulator.
- `OfflineModelManager`, `LocalASRModelManager`, and `LocalTTSModelManager` readiness checks are covered by Swift tests, including Android-compatible missing-item text, Qwen 4B/1.5B text model layouts, component-level offline vision/text inference readiness, Vosk required-file inspection, Vosk ZIP deflate extraction, zip-slip rejection, Vosk transcript parser spacing, and Android-compatible bert-vits2-MNN TTS required-file inspection.
- `WebAssetLocator`, native CoreMotion fall confirmation, iOS offline model preparation settings, iPad-safe action sheets, expanded native settings parity, CHW vision tensor preprocessing, dynamic MNN runtime symbol probing, environment/app-bundle MNN loading, and optional native runtime artifact embedding have been integrated into the app shell.
- Android `LocalModelBenchmarkActivity` is partially mirrored on iOS by a native local model diagnostics action and bridge-callable benchmark branches. It now generates per-test reports for status, ASR, vision, text, text suite, text inquiry, TTS, and manual scenario flows; every report carries the app's current `diagnostic_log_path`, `native_camera`, `native_speech`, and `native_tts` payloads so benchmark output can be correlated with native/JS JSONL diagnostics, camera permission/hardware/error state, microphone authorization, ASR runtime mode, recorder state, local ASR readiness, TTS runtime mode, system-speaking state, DashScope/native audio playback state, and local MNN TTS readiness during simulator and device smoke runs. Simulator automation prepares the Vosk model, 16kHz real-voice fixture, real-scene image, and DAMO-YOLO detector so `asr` must succeed with non-empty transcripts and `yolo_ready` is validated separately from MNN runtime availability. Strict simulator report validation now also requires the diagnostic log path, native camera payload, native speech payload, native TTS payload, component-level `vision_ready` / `text_inference_ready` fields, and the scenario-level ASR component to run successfully when the Vosk fixture is present. The checker now enforces ready semantics: if vision/text/TTS report ready they must also produce successful benchmark runs, and ready local TTS must create readable WAV files. TTS reports include real model-directory/runtime status instead of a hard-coded skip, and `scenario` reports expose manual `real_voice.wav` / `real_scene.jpg` readiness plus component-level ASR/vision/pipeline skip state. Real successful vision/text/TTS and full scenario benchmark runs still require iOS MNN/MNN TTS execution on device or simulator-compatible MNN runtime slices plus voice-quality evidence.

## Current validation limitation

Full Xcode, the iPhone simulator, and the connected iPhone are now usable for automated validation. The latest Key-injected umbrella migration run passed all cloud-first gates, including live DashScope text/vision/ASR/TTS scenarios, simulator automation, signed device install/launch smoke, and device-summary validation. The latest physical-device UI debug run also passed launch, settings, AI details, management round trip, and start-navigation native camera flow on the unlocked iPhone. The remaining cloud-path work is interaction evidence rather than API/runtime wiring: capture richer real-camera preview comparisons and long-press microphone logs/screenshots. `npm run check:ios:device-ui-summary` requires a passed latest device UI run; `SILVERCARE_ALLOW_DEVICE_UI_BLOCKED=1 npm run check:ios:device-ui-summary` can be used only to verify that a blocked locked-device run was classified cleanly. Local Vosk/Qwen/DAMO-YOLO/bert-vits2-MNN validation is deferred and remains available as an explicit opt-in benchmark path.
